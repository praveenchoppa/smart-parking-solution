services:
  - type: web
    name: parkpulse-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 4 --bind 0.0.0.0:$PORT server:app
    envVars:
      - key: PORT
        value: 5000
